node_modules

dist
dist-dev
dist-main

# Wrangler / Cloudflare local state
.wrangler

# Real env files (contain secrets). .env.example is the committed template.
.env
.env.local
.env.dev
.env.main

# Build artifacts
backend/bin

# Agent worktrees
.claude/worktrees/
