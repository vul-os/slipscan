// Stub — owned by a Phase 3 frontend agent. Replace with the real page.
import { PageHeader } from "@/components/PageHeader";

export default function ReconcilePage() {
  return (
    <div className="p-6 md:p-10">
      <PageHeader title="Reconcile" description="Coming soon" />
      <p className="text-sm text-zinc-500 mt-6">Reconcile page — under construction.</p>
    </div>
  );
}
