// Stub — owned by a Phase 2 frontend agent. Replace with the real page.
import { PageHeader } from "@/components/PageHeader";

export default function LedgerPage() {
  return (
    <div className="p-6 md:p-10">
      <PageHeader title="Ledger" subtitle="Coming soon" />
      <p className="text-sm text-zinc-500 mt-6">Ledger page — under construction.</p>
    </div>
  );
}
